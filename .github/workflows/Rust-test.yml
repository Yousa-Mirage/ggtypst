name: Rust Test
on:
  push:
    branches: [main, master]
    paths:
      - .github/workflows/Rust-test.yml
      - src/**
  pull_request:
    branches: [main, master]
    paths:
      - .github/workflows/Rust-test.yml
      - src/**
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  cargo-test:
    name: cargo-test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v6

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-rtools@v2
        if: runner.os == 'Windows'

      - name: Install Rust toolchain (Unix)
        if: runner.os != 'Windows'
        uses: dtolnay/rust-toolchain@stable

      - name: Install Rust toolchain (Windows GNU)
        if: runner.os == 'Windows'
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable-x86_64-pc-windows-gnu

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src/rust -> target

      - name: Run cargo test (Unix)
        if: runner.os != 'Windows'
        run: cargo test --manifest-path src/rust/Cargo.toml

      - name: Run cargo test (Windows GNU)
        if: runner.os == 'Windows'
        run: cargo test --manifest-path src/rust/Cargo.toml --target x86_64-pc-windows-gnu
